#include <stdio.h>
int main(void)
int
{
    if (n < 0)
    {
        printf("%d is negatif", n);
    } else if (n > 0) {
        printf("%d is positif", n);
    } else {
        printf("%n is zero", n)
    }
}