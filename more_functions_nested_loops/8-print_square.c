#include"main.h"
/**
* print_square - prints n squares
* @n: the number of squares
* Return: Always 0.
*/
void print_square(int size)
{
int x, y;
if (size <= 0)
{
_putchar('\n');
}
else
{
for (x = 0; x < n; x++)
{
for (y = 0; y < x; y++)
{
_putchar(35);
}
_putchar('\n');
}
}
}
